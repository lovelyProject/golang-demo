services:
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DATA: /data/postgres
    ports:
      - 5432:5432
    volumes:
      - ./postgres_data:/data/postgres
